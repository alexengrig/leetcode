rootProject.name = 'leetcode'

file('problems').eachDir { problemDir ->
    def javaDir = new File(problemDir, 'java')

    if (javaDir.isDirectory()) {
        include(":problems:${problemDir.name}")
        project(":problems:${problemDir.name}").projectDir = problemDir
    }
}
