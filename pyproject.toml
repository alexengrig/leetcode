[project]
name = "leetcode"
version = "0"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
doc = [
    "mkdocs",
    "mkdocs-material",
    "pyyaml",
]
